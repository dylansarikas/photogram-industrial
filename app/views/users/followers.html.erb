<%= render "users/pill_bar" %>

<div class="row">
  <div class="col-md-6 offset-md-3">
    <ul class="list-group">
      <% @user.followers.each do |follower| %>
        <li class="list-group-item"><%= follower.username %></li>
      <% end %>
    </ul>
  </div>
</div>
